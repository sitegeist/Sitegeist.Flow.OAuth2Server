Sitegeist:
  Flow:
    OAuth2Server:
      # (absolute) path to the private key
      privateKeyPath: ''
      # encryption key
      encryptionKey: ''
      ttls:
        accessToken: 'PT1H'
        authCode: 'PT10M'
        refreshToken: 'P1M'
      authenticationProvider: ''
